import { GlobalState } from ".";

export const musicDisc = (state: GlobalState) => state.musicDisc;
export const userInfo = (state: GlobalState) => state.user;
export const isLogin = (state: GlobalState) => state.isLogin;
export const currentMediaRef = (state: GlobalState) => state.currentMediaRef;
