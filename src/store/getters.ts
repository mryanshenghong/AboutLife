import { RootState } from '.'

export const musicDisc = (state: RootState) => state.musicDisc
export const userInfo = (state: RootState) => state.user
export const isLogin = (state: RootState) => state.isLogin
